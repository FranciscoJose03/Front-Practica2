import { FunctionComponent } from "preact";
import BPokemon from "../islands/BPokemon.tsx";

const PokemonL = () => {  
  return(
    <div>
        <BPokemon/>
    </div>
  );
}

export default PokemonL;